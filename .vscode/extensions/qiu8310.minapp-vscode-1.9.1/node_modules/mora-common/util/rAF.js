"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function rAF(fn) {
    var w = window;
    var canceled;
    var request = w.requestAnimationFrame
        || w.webkitRequestAnimationFrame
        || w.mozRequestAnimationFrame
        || w.msRequestAnimationFrame
        || w.oRequestAnimationFrame
        || (function (f) { return setTimeout(f, 16); });
    var cb = function () {
        if (!canceled && fn() !== false)
            request(cb);
    };
    request(cb);
    return {
        destroy: function () { return (canceled = true); }
    };
}
exports.rAF = rAF;
