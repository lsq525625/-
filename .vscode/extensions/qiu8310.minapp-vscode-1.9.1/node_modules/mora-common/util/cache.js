"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function cache(map, key, disable, callback) {
    // 不能这样写，一定要存到 map 中，有可能其它程序需要读取
    // if (disable) return callback()
    if (disable || !(key in map)) {
        map[key] = callback();
    }
    return map[key];
}
exports.cache = cache;
