/**
 * Use Array.reduce() to pass value through functions
 *
 * @example
 *
 * pipe(btoa, x => x.toUpperCase())("Test") -> "VGVZDA=="
 */
export declare function pipe(...funcs: Array<(arg: any) => any>): (arg: any) => any;
