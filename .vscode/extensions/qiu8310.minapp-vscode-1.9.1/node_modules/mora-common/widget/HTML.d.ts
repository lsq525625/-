/// <reference types="react" />
import * as React from 'react';
export interface IHTMLPRops extends React.HTMLAttributes<HTMLSpanElement> {
    /** 多行的话只显示指定的行数（注意，需要引入一个 lineClamp 类，可以在 style/base/common 中找到 ） */
    lineClamp?: number;
    /** 是否 html 内容来自于 CKEditor 组件生成的，是的话会给当前 html 组件加上一个 ck 的 className */
    fromEditor?: boolean;
    value?: string;
    children?: string;
    /** 简单模式，只处理 换行 和 空格 */
    simple?: boolean;
}
export declare class HTML extends React.PureComponent<IHTMLPRops, any> {
    render(): JSX.Element;
}
export declare function parseSimpleHTML(text: string): JSX.Element[];
