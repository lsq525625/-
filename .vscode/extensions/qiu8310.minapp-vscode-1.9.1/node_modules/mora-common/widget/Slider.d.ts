/// <reference types="react" />
import * as React from 'react';
import './style/Slider.scss';
export interface ISliderProps {
    className?: string;
    style?: React.CSSProperties;
    loop?: boolean;
    start?: number;
    direction?: 'horizontal' | 'vertical';
    animation?: string;
    offset?: number;
    afterChange?: (to: number) => void;
    beforeChange?: (from: number) => void;
}
export declare class Slider extends React.PureComponent<ISliderProps, any> {
    static defaultProps: Partial<ISliderProps>;
    isSliding: boolean;
    state: {
        total: number;
        reverse: boolean;
        currentSlide: number;
    };
    private slider;
    private canSlideNext;
    private canSlidePrev;
    slideTo(index: number, reverse?: boolean | null): void;
    nextSlide(): void;
    prevSlide(): void;
    private readonly slideStyle;
    private readonly child;
    render(): JSX.Element;
    private beforeChildEnter(el);
    private afterChildEnter(el);
    private beforeChildLeave(el);
    private onTouchStart();
    private onSwipe(e);
}
