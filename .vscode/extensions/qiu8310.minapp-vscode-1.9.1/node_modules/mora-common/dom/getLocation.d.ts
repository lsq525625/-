import { IUrlQuery } from '../util/url';
export interface ILocation {
    url: string;
    search: string;
    pathname: string;
    nativeQuery: IUrlQuery;
    query: IUrlQuery;
    hash: string;
}
export interface ILocationOptions {
    hash?: boolean;
    /** 默认是 "#" */
    separater?: string;
}
export declare function getLocation(options: ILocationOptions): ILocation;
