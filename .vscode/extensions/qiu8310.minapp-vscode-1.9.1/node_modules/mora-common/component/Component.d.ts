/// <reference types="react" />
import * as React from 'react';
export declare type IComponentProps<P = {}, DP = {}> = Readonly<{
    children?: React.ReactNode;
}> & Readonly<P> & Readonly<DP>;
/**
 * 如果组件下不会嵌入其它子组件，则建议不要使用 Component
 */
export declare class Component<P = {}, DP extends Partial<P> = {}, S = {}> extends React.Component<P, S> {
    props: IComponentProps<P, DP>;
}
