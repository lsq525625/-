/// <reference types="react" />
import * as React from 'react';
/**
 * 有些组件需要取到父组件的 dom
 *
 * 这时就需要等到自己完全加载完（即父组件也加载完，不考虑父组件中有异步的情况）
 */
export declare function renderAfterDidMount(options?: any): (Component: React.ComponentClass<{}>) => any;
