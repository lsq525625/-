/// <reference types="react" />
import * as React from 'react';
export interface Scrollable {
    scrollTop?: number;
    getScrollableContainer?: () => HTMLElement;
}
export declare abstract class Scrollable extends React.PureComponent<any, any> {
    /**
     * 在继承的子类添加上
     *
     * ```
     * scrollTop: number
     * ```
     *
     * 就可以用它去获取或设置页面的 scrollTop
     */
    static apply(): (Ctor: any) => any;
}
