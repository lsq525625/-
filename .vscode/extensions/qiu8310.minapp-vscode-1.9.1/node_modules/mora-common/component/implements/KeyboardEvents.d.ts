/// <reference types="react" />
import * as React from 'react';
export interface KeyboardEvents {
    __keyboardEventOff?: any;
}
export declare abstract class KeyboardEvents extends React.PureComponent<any, any> {
    /**
     * 在继承类上写
     *
     * ```
     * keyboard = {
     *   'ctrl+t': (e: KeyboardEvent) => void
     * }
     * ```
     */
    static apply(): (Ctor: any) => any;
    abstract keyboard: {
        [key: string]: (e: KeyboardEvent, key: string) => boolean | void;
    };
}
